# ============================================================
# Upstream: Next.js application
# ============================================================
upstream nextjs_upstream {
    server app:3000;
    keepalive 64;
}

# ============================================================
# HTTP → HTTPS redirect + ACME challenge
# ============================================================
server {
    listen 80;
    listen [::]:80;
    server_name pragunpai.online www.pragunpai.online;

    # Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all other HTTP traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# ============================================================
# HTTPS server (uncomment after obtaining SSL certificates)
#
# To obtain certificates for the first time, run:
#   docker compose run --rm certbot certonly \
#     --webroot --webroot-path=/var/www/certbot \
#     -d pragunpai.online -d www.pragunpai.online
#
# Then uncomment this block and restart nginx:
#   docker compose restart nginx
# ============================================================

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name pragunpai.online www.pragunpai.online;
#
#     # ----------------------------------------------------------
#     # SSL certificates (Let's Encrypt)
#     # ----------------------------------------------------------
#     ssl_certificate     /etc/letsencrypt/live/pragunpai.online/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pragunpai.online/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_session_tickets off;
#
#     # ----------------------------------------------------------
#     # Security headers
#     # ----------------------------------------------------------
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
#     add_header X-Frame-Options DENY always;
#     add_header X-Content-Type-Options nosniff always;
#     add_header Referrer-Policy strict-origin-when-cross-origin always;
#
#     # ----------------------------------------------------------
#     # Upload size limit
#     # ----------------------------------------------------------
#     client_max_body_size 10M;
#
#     # ----------------------------------------------------------
#     # Gzip compression
#     # ----------------------------------------------------------
#     gzip on;
#     gzip_vary on;
#     gzip_proxied any;
#     gzip_comp_level 6;
#     gzip_min_length 256;
#     gzip_types
#         text/plain
#         text/css
#         text/javascript
#         application/javascript
#         application/json
#         application/xml
#         image/svg+xml
#         font/woff2;
#
#     # ----------------------------------------------------------
#     # Next.js static assets — immutable, cached 1 year
#     # ----------------------------------------------------------
#     location /_next/static {
#         proxy_pass http://nextjs_upstream;
#         add_header Cache-Control "public, max-age=31536000, immutable";
#     }
#
#     # ----------------------------------------------------------
#     # Main proxy to Next.js
#     # CRITICAL: Buffering MUST be disabled for streaming
#     # (Server Components, Server Actions, after(), ISR)
#     # ----------------------------------------------------------
#     location / {
#         proxy_pass http://nextjs_upstream;
#
#         # Disable buffering for Next.js streaming support
#         proxy_buffering off;
#         proxy_cache off;
#         add_header X-Accel-Buffering no;
#
#         # Standard proxy headers
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $host;
#         proxy_set_header X-Forwarded-Port $server_port;
#
#         # WebSocket support
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#
#         # Timeouts
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }
